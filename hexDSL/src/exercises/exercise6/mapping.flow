@context()
@include( 'exercises/exercise5/conversion.flow' )
{
    mapping = mapping()

    mappings = new Array<hex.di.mapping.MappingDefinition>
    (
        mapping( { fromType: "Array<exercises.support.Character>->Void", toValue:portal.jump }), 

    )

    action = new Act("name", mappings)
}